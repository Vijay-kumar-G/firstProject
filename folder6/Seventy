class Solution {
    public int findNumbers(int[] nums) {
        int count = 0;
        for(int x:nums){
            int length = String.valueOf(x).length();
            if(length%2==0){
                count++;
            }
        }
        return count;
    }
}
